const config = {
    // 启动的端口号
    port: 3000,
    database: {
        HOST: 'localhost',
        USER: 'root',
        PASSWORD: 'cyx1314520',
        WALL: 'WALLTS'
    }
}

module.exports = config;
